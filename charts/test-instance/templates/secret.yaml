apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ include "test-instance.fullname" . }}-secret
  namespace: {{ .Release.Namespace }}
spec:
  data: []
  secretStoreRef:
    kind: ClusterSecretStore
    name: aws-secretsmanager-secret-store
  target:
    name: {{ include "test-instance.fullname" . }}-secret
    template:
      metadata: {}

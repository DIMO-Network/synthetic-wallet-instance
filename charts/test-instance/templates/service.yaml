apiVersion: v1
kind: Service
metadata:
  name: {{ include "synthetic-device-wallet-instance-api.fullname" . }}
  labels:
    {{- include "synthetic-device-wallet-instance-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  {{- range $key, $value := .Values.service.ports }}
    - name: {{ $key }}
{{ toYaml $value | indent 6 }}
  {{- end }}
  selector:
    {{- include "synthetic-device-wallet-instance-api.selectorLabels" . | nindent 4 }}

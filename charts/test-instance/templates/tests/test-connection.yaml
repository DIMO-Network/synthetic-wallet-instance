apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "synthetic-device-wallet-instance-api.fullname" . }}-test-connection"
  labels:
    {{- include "synthetic-device-wallet-instance-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "synthetic-device-wallet-instance-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
